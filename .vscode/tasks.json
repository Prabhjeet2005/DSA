{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run C++ Executable",
            "type": "shell",
            "command": "./${fileBasenameNoExtension}",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": []
        }
    ]
}
